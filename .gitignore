# ── dependencies & build output (regenerate with `npm install` / `npm run build`) ──
node_modules/
dist/

# ── generated / server-hosted assets ──
# Voice clips are generated by _build/gen-audio.mjs and served from the box; the
# Axo Rhymes catalog (rhymes.json) is refreshed on the server by a cron.
audio-clips/
public/rhymes.json
public/audio/

# ── secrets / environment (NEVER commit) ──
.env
.env.*
*.local
rhymes.env

# ── local tooling / OS noise ──
.claude/
.DS_Store
Thumbs.db
*.log
npm-debug.log*
