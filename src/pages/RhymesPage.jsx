// /rhymes — Axo Rhymes (curated kids' songs). Deep-links into the Learn app's
// video room so it shares the same immersive stage + theming.
import LearnPage from "./LearnPage";

export default function RhymesPage() {
  return <LearnPage initialScreen="videos" />;
}
